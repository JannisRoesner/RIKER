version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: riker:local
    container_name: riker
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./prints:/app/prints
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:3000/api/menu', (r)=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\" "]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s